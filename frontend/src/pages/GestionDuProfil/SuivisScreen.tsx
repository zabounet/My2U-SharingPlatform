import {
  View,
  Text,
  StyleSheet,
  ScrollView,
  SafeAreaView,
  KeyboardAvoidingView,
  Pressable,
} from "react-native";
import color from "../../../public/color.js";


const SuivisScreen = () => {
  return (
  <Text> Suivis </Text>
  );
};

const styles = StyleSheet.create({
  
});

export default SuivisScreen;
